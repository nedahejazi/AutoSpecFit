# AutoSpecFit GitHub upload commands

cd ~/Desktop/AutoSpecFit

git init
git add .
git commit -m "Initial release of AutoSpecFit v1.0"

# Create an empty GitHub repository named AutoSpecFit.
# Do not initialize it with README, license, or .gitignore.

git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/AutoSpecFit.git
git push -u origin main
